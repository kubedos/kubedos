global:
  scrape_interval: 15s

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
{% for h in groups['all'] %}
{% if hostvars[h].node_exporter_ip is defined %}
          - "{{ hostvars[h].node_exporter_ip }}:{{ node_exporter_port }}"
{% endif %}
{% endfor %}
