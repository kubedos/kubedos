---
# Compatibility wrapper:
# site.yml expects playbooks/02_common.yml, but the repo provides 01_common.yml.
# Keep this file so older site.yml references continue to work.
- import_playbook: 01_common.yml

